% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate-internal.R, R/interpolate.R
\name{.check_input}
\alias{.check_input}
\alias{.check_output}
\alias{monot}
\alias{get_nearest_index}
\alias{get_bilinear_weights}
\alias{interpolate-internal}
\alias{interp}
\title{Validate interpolation inputs and classify their geometry}
\usage{
.check_input(x, y, z)

.check_output(x, y)

monot(x, side)

get_nearest_index(x, y, xout, yout, mask = NULL, extrap = FALSE)

get_bilinear_weights(x, y, xout, yout, mask = NULL, extrap = FALSE)

interp(z, x, y, xout, yout, FUN, extrap = FALSE, control = list(), ...)
}
\arguments{
\item{x}{Numeric vector or matrix.}

\item{z}{Input matrix or array.}

\item{side}{Margin over which monotonicity is checked for matrix inputs.}

\item{xout, yout}{Output coordinates.}

\item{mask}{Optional mask on the source grid. Entries equal to `0` are treated
as missing. When `mask` is supplied, weights associated with masked cells are
either rejected or redistributed depending on `extrap`.}

\item{extrap}{Logical; passed to `.interp()`.}

\item{FUN}{Interpolation helper function.}

\item{control}{Named list of internal control options.}

\item{...}{Additional arguments passed to `FUN`.}

\item{x, y}{Input coordinates.}
}
\value{
A list with components:
\describe{
  \item{`case`}{A named logical vector with entries `is_gridR`, `is_gridI`,
  and `is_point`.}
  \item{`hasNA`}{Logical; indicates whether incomplete cases were detected.}
  \item{`data`}{A data frame of complete cases when missing values are present,
  otherwise `NULL`.}
}

A list with a single component `case`, a named logical vector with
  entries `is_gridR`, `is_gridI`, and `is_point`.

Logical scalar.

A two-column integer matrix giving the x- and y-indices of the nearest
  source locations.

A sparse matrix of bilinear weights with one row per output location
  and one column per source grid cell. The `"invalid"` attribute stores the
  indices of target locations that could not be interpolated.

An array of interpolated values whose first dimensions correspond to
  the requested output geometry and whose remaining dimensions match the
  trailing dimensions of `z`.
}
\description{
`.check_input()` validates the spatial structure of `x`, `y`, and `z`, and
identifies whether they describe a regular grid, an irregular grid, or point
data. When incomplete cases are present, complete observations are extracted
and returned for downstream use.

`.check_output()` validates `xout` and `yout` and identifies whether the
requested output corresponds to a regular grid, an irregular grid, or paired
point locations.

`monot()` checks whether a vector is strictly increasing, or whether all rows
or columns of a matrix are strictly increasing.

`get_nearest_index()` maps target coordinates to the nearest grid-cell indices
used by nearest-neighbour interpolation on regular grids.

`get_bilinear_weights()` constructs the sparse weight matrix used for bilinear
interpolation from a regular source grid to target locations.

Developer-oriented helper functions used to apply interpolation methods over
one or more layers and to manage optional transformations and error handling.

`interp()` applies `.interp()` over all layers of `z` beyond the first two
spatial dimensions and reconstructs the output array.
}
\keyword{internal}
