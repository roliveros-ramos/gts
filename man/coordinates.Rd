% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinates.R
\name{coordinates}
\alias{coordinates}
\alias{longitude}
\alias{longitude.gts}
\alias{longitude.static}
\alias{longitude.grid}
\alias{latitude}
\alias{latitude.gts}
\alias{latitude.static}
\alias{latitude.grid}
\alias{longitude<-}
\alias{longitude<-.gts}
\alias{longitude<-.static}
\title{Extract or modify horizontal coordinates of gridded objects}
\usage{
longitude(x, prime_meridian = NULL, ...)

\method{longitude}{gts}(x, prime_meridian = NULL, ...)

\method{longitude}{static}(x, prime_meridian = NULL, ...)

\method{longitude}{grid}(x, prime_meridian = NULL, ...)

latitude(x, ...)

\method{latitude}{gts}(x, ...)

\method{latitude}{static}(x, ...)

\method{latitude}{grid}(x, ...)

longitude(x) <- value

\method{longitude}{gts}(x) <- value

\method{longitude}{static}(x) <- value
}
\arguments{
\item{x}{A `gts`, `static`, or `grid` object.}

\item{prime_meridian}{Optional prime meridian convention used by
`longitude()`. Supported values are `"center"` for longitudes centred on
`[-180, 180]` and `"left"` for longitudes on `[0, 360]`. If omitted, the
convention is inferred from the stored longitude values and recorded in the
`"pm"` attribute of the returned object.}

\item{...}{Additional arguments passed to the method.}

\item{value}{Replacement longitude values for `longitude<-`.}
}
\value{
Depending on the method:
\describe{
  \item{`longitude()`}{The longitude coordinate vector or matrix. The
  returned object has a `"pm"` attribute indicating the prime meridian
  convention.}
  \item{`latitude()`}{The latitude coordinate vector or matrix.}
  \item{`longitude<-`}{The modified object.}
}
}
\description{
These methods provide access to the horizontal coordinates stored in gridded
objects.
}
\details{
`longitude()` and `latitude()` extract the horizontal coordinate definitions
from `gts`, `static`, and `grid` objects.

`longitude<-` replaces the longitude coordinate of regular-grid `gts` and
`static` objects and reorders the associated data and grid components
accordingly.


`longitude()` returns the stored longitude coordinates. When
`prime_meridian` is supplied, the returned values are converted to the
requested convention using the package longitude helpers.

`latitude()` returns the stored latitude coordinates unchanged.

`longitude<-.gts()` and `longitude<-.static()` currently support only regular
grids, that is, objects whose longitude coordinate is stored as a vector
rather than as a matrix. The replacement values must have the same length as
the original longitude coordinate and must not contain missing values.

When longitude values are replaced, the methods reorder the data array and
associated grid components to keep the object aligned with the new longitude
ordering. They also update the longitude breaks and selected grid metadata.
}
\section{Functions}{
\itemize{
\item \code{longitude(gts)}: Extract the longitude coordinates of a `gts` object.

\item \code{longitude(static)}: Extract the longitude coordinates of a `static`
object.

\item \code{longitude(grid)}: Extract the longitude coordinates of a `grid` object.

\item \code{latitude(gts)}: Extract the latitude coordinates of a `gts` object.

\item \code{latitude(static)}: Extract the latitude coordinates of a `static`
object.

\item \code{latitude(grid)}: Extract the latitude coordinates of a `grid` object.

\item \code{longitude(gts) <- value}: Replace the longitude coordinates of a regular-grid
`gts` object.

\item \code{longitude(static) <- value}: Replace the longitude coordinates of a regular-grid
`static` object.

}}
\examples{
\dontrun{
lon <- longitude(x)
lon360 <- longitude(x, prime_meridian = "left")
lat <- latitude(x)

longitude(x) <- seq(-80, -70, length.out = length(longitude(x)))
}
}
\seealso{
[grid-class], [gts-class], [static-class], [transform_coordinates()]
}
