% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gridded-inspect.R
\name{gridded-inspect}
\alias{gridded-inspect}
\alias{is.na.gts}
\alias{is.na.static}
\alias{dim.gts}
\alias{dim.static}
\alias{dim.grid}
\alias{print.gts}
\alias{print.static}
\alias{print.grid}
\alias{names.gts}
\alias{names.static}
\alias{names<-.gts}
\alias{names<-.static}
\alias{units.gts}
\alias{units.static}
\alias{units<-.gts}
\alias{units<-.static}
\alias{str.gts}
\alias{str.static}
\alias{is_climatology}
\alias{is_climatology.gts}
\title{Inspect and modify gridded objects}
\usage{
\method{is.na}{gts}(x)

\method{is.na}{static}(x)

\method{dim}{gts}(x)

\method{dim}{static}(x)

\method{dim}{grid}(x)

\method{print}{gts}(x, ...)

\method{print}{static}(x, ...)

\method{print}{grid}(x, ...)

\method{names}{gts}(x)

\method{names}{static}(x)

\method{names}{gts}(x) <- value

\method{names}{static}(x) <- value

\method{units}{gts}(x)

\method{units}{static}(x)

\method{units}{gts}(x) <- value

\method{units}{static}(x) <- value

\method{str}{gts}(object, ...)

\method{str}{static}(object, ...)

is_climatology(x, ...)

\method{is_climatology}{gts}(x, ...)
}
\arguments{
\item{x}{A `gts`, `static`, or `grid` object, depending on the method.}

\item{...}{Additional arguments passed to the underlying generic. These are
mainly relevant for `print.*()` and `str.*()`.}

\item{value}{For `names<-` methods, a character vector giving the variable
identifier and long name. For `units<-` methods, a character scalar giving
the data-variable units.}

\item{object}{A `gts` or `static` object.}
}
\value{
Depending on the method:
\describe{
  \item{`is.na.gts()`, `is.na.static()`}{An object of the same class, with
  `x$x` replaced by a logical array of missing-value indicators.}
  \item{`dim.gts()`, `dim.static()`, `dim.grid()`}{An integer vector of
  dimensions.}
  \item{`print.gts()`, `print.static()`, `print.grid()`, `str.gts()`,
  `str.static()`}{The object is printed for inspection and the function
  returns `invisible(NULL)`.}
  \item{`names.gts()`, `names.static()`}{A character vector of length two
  containing the variable identifier and long name.}
  \item{`names<-.gts()`, `names<-.static()`}{The modified object.}
  \item{`units.gts()`, `units.static()`}{A character scalar giving the
  data-variable units.}
  \item{`units<-.gts()`, `units<-.static()`}{The modified object.}
  \item{`is_climatology()`}{A logical scalar.}
}
}
\description{
These methods provide compact inspection tools for gridded objects and access
to selected metadata fields stored in `gts` and `static` objects.
}
\details{
The page covers three related classes:
\itemize{
  \item `gts` objects, which store gridded time-series data;
  \item `static` objects, which store gridded spatial fields without a time
  dimension;
  \item `grid` objects, which store spatial geometry and associated metadata.
}

`dim.gts()` and `dim.static()` return the dimensions of the data array stored
in `x$x`. `dim.grid()` returns the dimensions of the horizontal grid stored in
`x$LON`.

`is.na.gts()` and `is.na.static()` return a copy of the input object with
`x$x` replaced by a logical array indicating missing values.

`print.gts()`, `print.static()`, and `print.grid()` print a compact summary
of the object, including variable identifier, units, spatial extent, and grid
resolution. For `gts` objects, the printed summary also includes time
coverage and frequency.

`names.gts()` and `names.static()` return the variable identifier and long
name stored in the metadata. The corresponding replacement methods update
those fields.

`units.gts()` and `units.static()` return the units of the data variable,
that is, the last entry of `x$info$units`. The corresponding replacement
methods update that value.

`str.gts()` and `str.static()` display the underlying list structure of the
object.

`is_climatology()` tests whether a `gts` object is marked as a climatology.


`names.gts()` and `names.static()` expose selected variable metadata rather
than dimension names. They return the variable identifier and long name stored
in the object's `info` component.

`units.gts()` and `units.static()` expose only the units of the data variable,
not the units of coordinate dimensions.

`str.gts()` and `str.static()` temporarily coerce the object to a plain list
before calling [utils::str()], so they display the underlying structure rather
than a shortened class-specific summary.

`is_climatology()` is only defined for `gts` objects, because climatology is a
time-series concept in this package.
}
\section{Functions}{
\itemize{
\item \code{is.na(gts)}: Identify missing values in a `gts` object.

\item \code{is.na(static)}: Identify missing values in a `static` object.

\item \code{dim(gts)}: Return the dimensions of the data array stored in
a `gts` object.

\item \code{dim(static)}: Return the dimensions of the data array stored in
a `static` object.

\item \code{dim(grid)}: Return the dimensions of the horizontal grid
stored in a `grid` object.

\item \code{print(gts)}: Print a compact summary of a `gts` object.

\item \code{print(static)}: Print a compact summary of a `static` object.

\item \code{print(grid)}: Print a compact summary of a `grid` object.

\item \code{names(gts)}: Return the variable identifier and long name
stored in a `gts` object.

\item \code{names(static)}: Return the variable identifier and long name
stored in a `static` object.

\item \code{names(gts) <- value}: Replace the variable identifier and long name
stored in a `gts` object.

\item \code{names(static) <- value}: Replace the variable identifier and long name
stored in a `static` object.

\item \code{units(gts)}: Return the data-variable units stored in a `gts`
object.

\item \code{units(static)}: Return the data-variable units stored in a
`static` object.

\item \code{units(gts) <- value}: Replace the data-variable units stored in a `gts`
object.

\item \code{units(static) <- value}: Replace the data-variable units stored in a
`static` object.

\item \code{str(gts)}: Display the underlying list structure of a `gts`
object.

\item \code{str(static)}: Display the underlying list structure of a
`static` object.

\item \code{is_climatology()}: Test whether a `gts` object is marked as a
climatology.

\item \code{is_climatology(gts)}: Test whether a `gts` object is marked as a
climatology.

}}
\examples{
\dontrun{
dim(x)
names(x)
units(x)
print(x)
str(x)
}
}
\seealso{
[gts-class], [static-class], [grid-class], [gts()], [read_gts()], [read_static()], [make_grid()]
}
